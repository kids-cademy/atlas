<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="@dimen/divider_size"
    android:background="@drawable/click_highlight"
    android:padding="8dp">

    <!-- view item from reader_related_objects -->

    <ImageView
        android:id="@+id/related_object_icon"
        android:layout_width="@dimen/icon_size"
        android:layout_height="@dimen/icon_size"
        android:contentDescription="@string/image_description"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:src="@drawable/sample_object_icon" />

    <TextView
        android:id="@+id/related_object_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="24dp"
        android:layout_marginTop="4dp"
        android:layout_marginEnd="8dp"
        android:textAppearance="@style/TextAppearance.Small"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/related_object_icon"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="@string/sample_object_name" />

    <TextView
        android:id="@+id/related_object_definition"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:singleLine="true"
        android:textAppearance="@style/TextAppearance.XSmall.Mute"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@id/related_object_name"
        app:layout_constraintStart_toStartOf="@id/related_object_name"
        app:layout_constraintTop_toBottomOf="@id/related_object_name"
        tools:text="@string/sample_object_description" />
</android.support.constraint.ConstraintLayout>
